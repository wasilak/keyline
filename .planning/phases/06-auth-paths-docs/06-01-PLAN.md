---
phase: 06-auth-paths-docs
plan: 01
type: execute
wave: 1
depends_on: [05-audit-logging]
files_modified:
  - docs/auth-paths.md
autonomous: true
requirements:
  - DOC-03

must_haves:
  truths:
    - "docs/auth-paths.md exists covering all five authentication paths"
    - "Each path has curl/http command examples and expected responses"
    - "Precedence table documents auth method evaluation order"
    - "Deployment-mode diagrams show forward-auth vs standalone topology"
    - "Audit log output examples (success + failure) included for each path"
  artifacts:
    - path: "docs/auth-paths.md"
      provides: "Complete auth path reference with test commands (DOC-03)"
---

<objective>
Create a comprehensive auth paths reference document covering all five auth methods:
session, basic, LDAP, OIDC, and unknown/fallthrough. Include curl examples, precedence table,
deployment topology diagrams, and expected audit log output.

Requirements satisfied: DOC-03
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/auth-paths.md</name>
  <action>
    - Create docs/auth-paths.md with:
      - Precedence table (auth method evaluation order)
      - Deployment-mode diagrams (forward-auth and standalone modes)
      - Per-path sections: session, basic, ldap, oidc, unknown
      - Each section: description, curl examples, expected headers, expected audit log (success + failure)
  </action>
  <done>docs/auth-paths.md created with all 5 paths, curl examples, diagrams, and audit log samples.</done>
</task>

</tasks>

<verification>
Phase complete when:
1. docs/auth-paths.md exists
2. All 5 auth paths covered (session, basic, ldap, oidc, unknown)
3. curl examples present for each path
4. Audit log output examples included
</verification>
