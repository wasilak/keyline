---
phase: 02-documentation-update
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docs/deployment/docker.md
  - docs/docs/user-management/role-mappings.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "docker.md Environment Variables table includes LDAP_BIND_PASSWORD"
    - "docker.md health check endpoint matches the actual path served by the v2.0 binary"
    - "role-mappings.md claim field description does not falsely restrict values to a closed enum"
  artifacts:
    - path: "docs/docs/deployment/docker.md"
      provides: "Accurate v2.0 deployment guide with LDAP env-var coverage"
    - path: "docs/docs/user-management/role-mappings.md"
      provides: "Accurate claim field description"
  key_links:
    - from: "docker.md env-var table"
      to: "internal/auth/ldap.go bind_password env-var enforcement"
      via: "documented LDAP_BIND_PASSWORD entry"
      pattern: "LDAP_BIND_PASSWORD"
---

<objective>
Close the remaining accuracy gaps in the deployment and role-mappings docs (D-02). Add LDAP-related env vars to the docker.md reference, reconcile the health-check endpoint with the actual server route, and tighten the role-mappings.md `claim` field description so it matches the unrestricted struct definition.

Purpose: Satisfies DOC-01 by completing the deployment-layer accuracy review.
Output: Two updated docs files with accurate v2.0 references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-documentation-update/02-CONTEXT.md
@.planning/phases/02-documentation-update/02-PATTERNS.md
@docs/docs/deployment/docker.md
@docs/docs/user-management/role-mappings.md
@internal/config/config.go
@internal/auth/ldap.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docker.md env-var table and reconcile health check endpoint</name>
  <files>docs/docs/deployment/docker.md</files>
  <read_first>
    - docs/docs/deployment/docker.md (full file — focus on line 19 image ref, lines 49 and 76 health check curl commands, lines 162 ES version pin, lines 230-237 env-var table)
    - internal/auth/ldap.go (bind_password env-var enforcement, lines 115-128)
    - internal/config/config.go (LDAPConfig and any other env-var sources)
    - The actual route registration in the keyline source (search internal/server/ or wherever HTTP routes are wired) to confirm the real health endpoint path — quick-start.md uses `/_health`, docker.md uses `/healthz`; only one can be right
    - .planning/phases/02-documentation-update/02-PATTERNS.md (docker.md section, lines 303-321)
  </read_first>
  <action>
    Three concrete corrections:

    1. **Add LDAP env-var row to the Environment Variables table** (around lines 230-237). Match the existing column layout exactly. Required row: `LDAP_BIND_PASSWORD` column 1, `No` (or whatever the existing "required" column header expects) column 2, `LDAP bind password (required when ldap.enabled is true)` column 3. If the existing table also lists URL/DN-style env vars for other systems, add matching rows for `LDAP_URL`, `LDAP_BIND_DN`, `LDAP_SEARCH_BASE`, `LDAP_GROUP_SEARCH_BASE` using the same column conventions.

    2. **Reconcile the health-check endpoint**. Read the actual route registration in the keyline source to determine the correct path. If the correct path is `/healthz`, leave docker.md alone (Plan 02 handles quick-start.md). If the correct path is `/_health`, replace `/healthz` on docker.md lines 49 and 76 with `/_health`. Record the verified path in the SUMMARY so downstream verification can confirm consistency across files.

    3. **Image reference sanity check** (line 19): confirm it reads `ghcr.io/wasilak/keyline:latest`. If it already does, no change needed. Do NOT change the Elasticsearch version pin on line 162 — the pattern map confirms it is intentional.
  </action>
  <verify>
    <automated>rg -c "LDAP_BIND_PASSWORD" docs/docs/deployment/docker.md | grep -qv '^0$' && rg -c "ghcr\\.io/wasilak/keyline" docs/docs/deployment/docker.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `rg "LDAP_BIND_PASSWORD" docs/docs/deployment/docker.md` matches at least once
    - `rg "ghcr\.io/wasilak/keyline" docs/docs/deployment/docker.md` matches at least once
    - The health-check curl commands on lines 49 and 76 use the same path as the actual server route (verified by reading internal/server/ or equivalent)
    - The Environment Variables table still contains its pre-existing rows (no accidental deletions): the row count of the env-var table is greater than or equal to its previous size plus 1
    - Elasticsearch image pin on line 162 is unchanged (Plan 02 pattern map confirms it is intentional)
  </acceptance_criteria>
  <done>docker.md documents the LDAP_BIND_PASSWORD env var, the health-check curl commands match the real server route, and the image reference is the correct ghcr.io/wasilak path.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten role-mappings.md claim field description</name>
  <files>docs/docs/user-management/role-mappings.md</files>
  <read_first>
    - docs/docs/user-management/role-mappings.md (full file — find the `claim` field description)
    - internal/config/config.go (lines 62-66 — RoleMapping struct, confirms `Claim string` has no enum constraint)
    - .planning/phases/02-documentation-update/02-PATTERNS.md (role-mappings.md section, lines 291-299)
  </read_first>
  <action>
    Single concrete edit: locate the description of the `claim` field in role-mappings.md. It currently says values are restricted to `groups` or `email`. The struct (`RoleMapping`, config.go lines 62-66) has `Claim string` with no enum constraint, so any OIDC claim name is accepted.

    Change the description so it says claim values are **typically** `groups` or `email` (or similar wording such as "commonly `groups` or `email`, but any claim name from the OIDC token is accepted"). Do NOT restructure the file or change any other content — pattern map explicitly notes this doc is otherwise accurate.
  </action>
  <verify>
    <automated>rg -i "typically|commonly|any claim" docs/docs/user-management/role-mappings.md | rg -i "groups|email"; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -i "typically|commonly|any claim" docs/docs/user-management/role-mappings.md` matches at least once in the same context as `groups` or `email`
    - The doc no longer contains absolute-restriction phrasing like "only `groups` or `email`" or "must be `groups` or `email`": `rg -i "only\s+\`?groups\`?\s+or\s+\`?email\`?|must\s+be\s+\`?groups\`?" docs/docs/user-management/role-mappings.md` exits 1
    - All other sections of the file (pattern matching logic, wildcard support, default_es_roles fallback, deny-if-no-match behaviour) are preserved: `rg "default_es_roles" docs/docs/user-management/role-mappings.md` still matches
  </acceptance_criteria>
  <done>role-mappings.md describes the claim field as unrestricted (with typical values noted) rather than enum-constrained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation accuracy fixes only. No code or runtime configuration changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | docker.md health check | accept | Wrong path in docs is a usability defect, not a security issue; corrected via source-of-truth verification. |
</threat_model>

<verification>
- `rg "LDAP_BIND_PASSWORD" docs/docs/deployment/docker.md` matches at least once
- Health-check path in docker.md matches the actual route registered in the keyline source
- `rg -i "only\s+\`?groups\`?\s+or\s+\`?email\`?" docs/docs/user-management/role-mappings.md` exits 1
</verification>

<success_criteria>
- docker.md env-var table covers LDAP_BIND_PASSWORD.
- docker.md health-check endpoint matches the real server route.
- role-mappings.md claim field description accurately reflects the unrestricted struct definition.
</success_criteria>

<output>
Create `.planning/phases/02-documentation-update/02-04-SUMMARY.md` when done.
</output>
