---
phase: 02-documentation-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docs/configuration.md
  - docs/docs/user-management/dynamic-user-management.md
  - docs/docs/getting-started/quick-start.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "configuration.md documents every top-level config section that exists in internal/config/config.go (server, server.cors, session, cache, oidc, ldap, upstream, user_management, observability, elasticsearch)"
    - "configuration.md has no reference to a `user_management.enabled` field (the field does not exist in the struct)"
    - "dynamic-user-management.md config snippet does not contain `enabled: true` under user_management"
    - "quick-start.md config snippet does not contain `enabled: true` under user_management"
    - "configuration.md cache table documents redis_password and redis_db"
    - "configuration.md server table documents max_concurrent and cors.allowed_origins"
    - "configuration.md session table documents cookie_path"
    - "configuration.md upstream table documents insecure_skip_verify"
    - "configuration.md contains an LDAP configuration section"
    - "configuration.md contains an Observability configuration section"
  artifacts:
    - path: "docs/docs/configuration.md"
      provides: "Accurate v2.0 config reference covering all struct fields"
    - path: "docs/docs/user-management/dynamic-user-management.md"
      provides: "Corrected user_management config snippet"
    - path: "docs/docs/getting-started/quick-start.md"
      provides: "Corrected user_management config snippet and verified health endpoint"
  key_links:
    - from: "configuration.md field names"
      to: "internal/config/config.go mapstructure tags"
      via: "snake_case names match struct tags verbatim"
      pattern: "user_management|tls_skip_verify|insecure_skip_verify"
---

<objective>
Fix accuracy bugs across three reference docs (D-01, D-02, D-03). The configuration.md reference is missing four entire sections (CORS, LDAP, Observability) and several rows (max_concurrent, cookie_path, redis_password/db, insecure_skip_verify). Three files reference a non-existent `user_management.enabled` field that must be removed. Source of truth throughout is `internal/config/config.go` (mapstructure tags) and `config/config.example.yaml`.

Purpose: Satisfies DOC-01 (accurate v2.0 feature documentation) for the docs reference layer.
Output: Three updated files with corrected, complete config reference content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-documentation-update/02-CONTEXT.md
@.planning/phases/02-documentation-update/02-PATTERNS.md
@docs/docs/configuration.md
@docs/docs/user-management/dynamic-user-management.md
@docs/docs/getting-started/quick-start.md
@internal/config/config.go
@config/config.example.yaml
@config/user-management-example.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix configuration.md missing sections and incorrect fields</name>
  <files>docs/docs/configuration.md</files>
  <read_first>
    - docs/docs/configuration.md (full file — pay close attention to Server, Session, Cache, Upstream, user_management tables and any LDAP/Observability/CORS sections)
    - internal/config/config.go (lines 8-184 — authoritative struct definitions and mapstructure tags)
    - config/config.example.yaml (canonical example values)
    - .planning/phases/02-documentation-update/02-PATTERNS.md (configuration.md section, lines 119-184)
  </read_first>
  <action>
    Source of truth is `internal/config/config.go`. Every field name must come from a mapstructure tag in that file. Apply the following nine concrete corrections:

    1. **Server table — add row** for `max_concurrent`: default `0 (unlimited)`, description `Max concurrent requests; 503 when exceeded`. Source: config.go line 29.

    2. **Add new CORS subsection** under Server Configuration. Document `server.cors.allowed_origins` (list of allowed origin domains, default empty list). Source: CORSConfig struct, config.go lines 34-39; example.yaml lines 33-43. Include a short YAML snippet showing `server.cors.allowed_origins` populated with one example origin using the `yourdomain.com` pattern from D-06.

    3. **Upstream table — add row** for `insecure_skip_verify`: default `false`, description `Skip TLS certificate verification (dev only)`. Source: config.go line 161.

    4. **Remove `user_management.enabled`** from the User Management table and any user_management YAML snippet in this file. The field does not exist in the struct (`UserMgmtConfig`, config.go lines 142-145 has only `password_length` and `credential_ttl`). Add a one-sentence note: user management activates automatically when `elasticsearch.admin_user` and `elasticsearch.admin_password` are configured.

    5. **Add new LDAP section** documenting all 15+ fields of `LDAPConfig` (config.go lines 84-119). Use the exact snake_case mapstructure tag names. Include a table covering: `enabled`, `url`, `bind_dn`, `bind_password`, `connection_timeout`, `tls_mode`, `tls_skip_verify`, `search_base`, `search_filter`, `group_search_base`, `group_search_filter`, `username_attribute`, `email_attribute`, `display_name_attribute`, `group_name_attribute`, `required_groups`, and `attribute_mapping`. State explicitly that `bind_password` must be supplied as a `${ENV_VAR}` reference (enforced by ldap.go). State the three valid `tls_mode` values: `none`, `ldaps`, `starttls`.

    6. **Session table — add row** for `cookie_path`: default `/`, description `Cookie path`. Source: config.go line 127.

    7. **Add new Observability section** documenting `ObservabilityConfig` (config.go lines 165-184). Include all fields with their mapstructure tags as the canonical names. Reference example.yaml for example values.

    8. **Cache table — add two rows**: `redis_password` (default empty, description `Redis authentication password`) and `redis_db` (default `0`, description `Redis database number (0-15)`). Source: config.go lines 133-134.

    9. **OIDC** — if configuration.md has no OIDC section, leave it alone (it lives in authentication/oidc-authentication.md). If it does have one and is missing `user_identity_claim`, add that field. Source: config.go line 50.

    Match the existing table styling exactly (column count, alignment, header row).
  </action>
  <verify>
    <automated>rg "user_management\.enabled|user_management:\s*\n\s*enabled" docs/docs/configuration.md; test $? -eq 1 && rg -c "max_concurrent" docs/docs/configuration.md | grep -qv '^0$' && rg -c "allowed_origins" docs/docs/configuration.md | grep -qv '^0$' && rg -c "redis_password" docs/docs/configuration.md | grep -qv '^0$' && rg -c "redis_db" docs/docs/configuration.md | grep -qv '^0$' && rg -c "cookie_path" docs/docs/configuration.md | grep -qv '^0$' && rg -c "insecure_skip_verify" docs/docs/configuration.md | grep -qv '^0$' && rg -ci "^##.*ldap" docs/docs/configuration.md | grep -qv '^0$' && rg -ci "^##.*observability" docs/docs/configuration.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `rg "enabled" docs/docs/configuration.md | rg -i "user_management"` returns nothing (stale field removed)
    - `rg "max_concurrent" docs/docs/configuration.md` matches at least once
    - `rg "allowed_origins" docs/docs/configuration.md` matches at least once
    - `rg -i "^##.*cors" docs/docs/configuration.md` matches at least once (CORS section header exists)
    - `rg "redis_password" docs/docs/configuration.md` matches at least once
    - `rg "redis_db" docs/docs/configuration.md` matches at least once
    - `rg "cookie_path" docs/docs/configuration.md` matches at least once
    - `rg "insecure_skip_verify" docs/docs/configuration.md` matches at least once
    - `rg -i "^##.*ldap" docs/docs/configuration.md` matches at least once (LDAP section header exists)
    - `rg "tls_mode" docs/docs/configuration.md` matches at least once
    - `rg "bind_dn" docs/docs/configuration.md` matches at least once
    - `rg "search_filter" docs/docs/configuration.md` matches at least once
    - `rg "required_groups" docs/docs/configuration.md` matches at least once
    - `rg -i "^##.*observability" docs/docs/configuration.md` matches at least once (Observability section header exists)
    - `rg "\$\{[A-Z_]+\}" docs/docs/configuration.md` matches at least once in the LDAP section (bind_password env-var pattern documented)
  </acceptance_criteria>
  <done>configuration.md covers every top-level config section present in `internal/config/config.go`; no stale `user_management.enabled` reference; field names match mapstructure tags verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale user_management.enabled from dynamic-user-management.md and quick-start.md</name>
  <files>docs/docs/user-management/dynamic-user-management.md, docs/docs/getting-started/quick-start.md</files>
  <read_first>
    - docs/docs/user-management/dynamic-user-management.md (full file — focus on the user_management config snippet around line 103)
    - docs/docs/getting-started/quick-start.md (full file — focus on user_management snippet around line 97 and health-check curl example around line 117)
    - internal/config/config.go (lines 142-145 — UserMgmtConfig has only password_length and credential_ttl)
    - internal/usermgmt/metrics.go (to confirm metric names referenced in dynamic-user-management.md are accurate)
    - The actual server route registration to confirm the real health endpoint path (`/healthz` vs `/_health`)
    - .planning/phases/02-documentation-update/02-PATTERNS.md (lines 279-301 and 324-336)
  </read_first>
  <action>
    Three concrete corrections, each touching a single file:

    1. **dynamic-user-management.md**: remove the `enabled: true` line from the `user_management:` YAML snippet (around line 103). Replace it with a one-sentence note immediately after the snippet stating that user management activates automatically when `elasticsearch.admin_user` is set. Verify metric names listed in the doc match the names registered in `internal/usermgmt/metrics.go`; correct any mismatches by replacing the doc name with the actual exported metric name.

    2. **quick-start.md user_management snippet** (around line 97): remove the `enabled: true` line. The snippet should show only `password_length` and `credential_ttl` (plus any other genuine UserMgmtConfig fields). Leave `upstream.insecure_skip_verify: true` (line ~105) and `elasticsearch.insecure_skip_verify: true` (line ~95) — those are correct for a dev quick-start.

    3. **quick-start.md health check** (around line 117): the `curl http://localhost:9000/_health` example with expected response `"version": "1.0.0"` is potentially wrong on two counts (path and version). Read the actual route registration in the keyline source to confirm the correct health endpoint path. Update the curl URL to that path. Update the expected version string to `2.0.0` (or whatever the current binary returns — if there is no version field in the health response, remove the version line entirely rather than fabricate one).
  </action>
  <verify>
    <automated>rg "enabled:\s*true" docs/docs/user-management/dynamic-user-management.md; test $? -eq 1 && rg -B1 "enabled:" docs/docs/getting-started/quick-start.md | rg -i "user_management"; test $? -eq 1 && rg "1\\.0\\.0" docs/docs/getting-started/quick-start.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "enabled:\s*true" docs/docs/user-management/dynamic-user-management.md` exits 1
    - `rg -A3 "^user_management:" docs/docs/user-management/dynamic-user-management.md | rg "enabled"` exits 1
    - `rg -A3 "^user_management:" docs/docs/getting-started/quick-start.md | rg "enabled"` exits 1
    - `rg "password_length|credential_ttl" docs/docs/user-management/dynamic-user-management.md` still matches (genuine fields preserved)
    - `rg "1\.0\.0" docs/docs/getting-started/quick-start.md` exits 1 (stale version string removed)
    - quick-start.md still contains a health-check example (the `curl` line is updated, not removed): `rg "curl.*localhost" docs/docs/getting-started/quick-start.md` matches at least once
    - dynamic-user-management.md still contains its mermaid diagrams and the AES-256-GCM reference: `rg "AES-256-GCM" docs/docs/user-management/dynamic-user-management.md` matches at least once
  </acceptance_criteria>
  <done>Neither file references the non-existent `user_management.enabled` field; quick-start health-check example reflects the actual v2.0 route and version; metric names in dynamic-user-management.md match the registered metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure documentation accuracy fixes. No code, schema, runtime config, or secret-handling logic is modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information disclosure | configuration.md LDAP section | mitigate | Explicitly document the `${ENV_VAR}` requirement for `bind_password` so readers do not put plaintext secrets into config files. |
| T-02-04 | Tampering | configuration.md field reference | accept | Stale field documentation (`user_management.enabled`) misleads operators but cannot itself cause security regressions; removing the stale reference is the mitigation. |
</threat_model>

<verification>
- `rg "user_management.enabled|enabled:\s*true" docs/docs/configuration.md docs/docs/user-management/dynamic-user-management.md docs/docs/getting-started/quick-start.md | rg -i "user_management"` returns nothing
- `rg -i "^##.*(cors|ldap|observability)" docs/docs/configuration.md` returns at least three matches (one per new section)
</verification>

<success_criteria>
- configuration.md covers every top-level section in `internal/config/config.go` with field names matching mapstructure tags.
- Three docs files no longer reference the non-existent `user_management.enabled` field.
- quick-start health-check example reflects the actual v2.0 route and version (or omits version entirely).
</success_criteria>

<output>
Create `.planning/phases/02-documentation-update/02-02-SUMMARY.md` when done.
</output>
