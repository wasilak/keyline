---
phase: 02-documentation-update
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docs/authentication/overview.md
  - docs/docs/authentication/ldap-authentication.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Authentication overview lists LDAP / AD as a supported method alongside OIDC and Basic Auth"
    - "A new LDAP authentication guide exists at docs/docs/authentication/ldap-authentication.md"
    - "The LDAP guide documents all three TLS modes (ldaps, starttls, none) with production guidance"
    - "The LDAP guide states that bind_password must be supplied as a ${ENV_VAR} reference"
    - "The LDAP guide includes both an Active Directory example and an OpenLDAP example"
    - "The LDAP guide explains username normalisation (trim, lower-case, unsupported-char removal)"
  artifacts:
    - path: "docs/docs/authentication/overview.md"
      provides: "Auth overview reflecting v2.0 LDAP support"
    - path: "docs/docs/authentication/ldap-authentication.md"
      provides: "New LDAP authentication guide (file created)"
  key_links:
    - from: "ldap-authentication.md TLS modes table"
      to: "internal/config/config.go LDAPConfig.TLSMode field"
      via: "documented values match the enum-style strings the code accepts"
      pattern: "ldaps|starttls|none"
    - from: "ldap-authentication.md bind_password section"
      to: "internal/auth/ldap.go env-var enforcement"
      via: "documented ${ENV_VAR} requirement"
      pattern: "\\$\\{[A-Z_]+\\}"
---

<objective>
LDAP authentication was added in v2.0 but is undocumented in the Docusaurus auth section (D-02, D-03). Update `authentication/overview.md` to include LDAP and create a new `authentication/ldap-authentication.md` guide modelled on the existing `local-users-basic-auth.md`.

Purpose: Satisfies DOC-01 by adding the missing authentication documentation for a headline v2.0 feature.
Output: One updated overview file and one new LDAP guide file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-documentation-update/02-CONTEXT.md
@.planning/phases/02-documentation-update/02-PATTERNS.md
@docs/docs/authentication/overview.md
@docs/docs/authentication/local-users-basic-auth.md
@internal/config/config.go
@internal/auth/ldap.go
@internal/auth/engine.go
@config/config.example.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update authentication/overview.md to include LDAP</name>
  <files>docs/docs/authentication/overview.md</files>
  <read_first>
    - docs/docs/authentication/overview.md (full file — focus on the method table around lines 12-15, any auth-flow mermaid diagram, and the Authentication Endpoints section)
    - internal/auth/engine.go (lines 1-80 — confirm auth priority order: Session → Basic Auth → LDAP fallback → OIDC redirect, and the ldapProvider/ldapEnabled fields on lines 26-28)
    - .planning/phases/02-documentation-update/02-PATTERNS.md (overview.md section, lines 187-209)
  </read_first>
  <action>
    Two concrete edits to overview.md:

    1. **Add an LDAP row to the auth-method table** (around lines 12-15). Match the existing table's column count and header style. Row content: `**LDAP / AD**` | `Corporate directory authentication` | `No (stateless)` | `Existing AD environments`.

    2. **Add a clarifying note to the Authentication Endpoints section** stating that LDAP has no dedicated endpoint: it reuses the Basic Auth header path (`Authorization: Basic`), and the auth engine selects the method automatically (local users checked first, then LDAP if no local match). If the file contains a mermaid auth-flow diagram that shows `CheckBasic` → local user lookup, extend that branch with a textual note (or add a diagram node) describing the LDAP fallback. If extending the diagram would require non-trivial restructuring, add a paragraph under the diagram that states the actual order verbatim: Session → Basic Auth header (local users → LDAP fallback) → OIDC redirect.

    Source for the priority order is `internal/auth/engine.go` lines 1-80 — the doc text must match what the code actually does.
  </action>
  <verify>
    <automated>rg -c "LDAP" docs/docs/authentication/overview.md | grep -qv '^0$' && rg -c "Corporate directory" docs/docs/authentication/overview.md | grep -qv '^0$' && rg -ci "Authorization:\\s*Basic" docs/docs/authentication/overview.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `rg "LDAP" docs/docs/authentication/overview.md` matches at least 2 times (table row + endpoints note)
    - `rg "Corporate directory" docs/docs/authentication/overview.md` matches at least once
    - `rg -i "Authorization:\s*Basic" docs/docs/authentication/overview.md` matches at least once (clarifying note explains shared header path)
    - `rg "local users? checked first|local.*then LDAP|LDAP fallback" -i docs/docs/authentication/overview.md` matches at least once (auth priority documented)
    - The existing OIDC and Basic Auth rows in the method table are preserved (no rows removed): `rg "OIDC" docs/docs/authentication/overview.md` and `rg "Basic Auth" docs/docs/authentication/overview.md` both still match
  </acceptance_criteria>
  <done>Overview lists LDAP as a v2.0 auth method, the auth priority order matches `internal/auth/engine.go`, and the shared `Authorization: Basic` header behaviour is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Create new docs/docs/authentication/ldap-authentication.md guide</name>
  <files>docs/docs/authentication/ldap-authentication.md</files>
  <read_first>
    - docs/docs/authentication/local-users-basic-auth.md (the analog file to copy structure from — frontmatter pattern, section headings, code-block style)
    - internal/auth/ldap.go (full file — username normalisation lines 225-232, bind_password enforcement lines 115-128, group search behaviour, attribute mapping behaviour)
    - internal/config/config.go (lines 84-119 — LDAPConfig struct, all mapstructure tags)
    - config/config.example.yaml (lines 143-210 main LDAP block and lines 538-560 AD/OpenLDAP examples)
    - .planning/phases/02-documentation-update/02-PATTERNS.md (ldap-authentication.md section, lines 212-275)
  </read_first>
  <action>
    Create a new file at `docs/docs/authentication/ldap-authentication.md`. Copy the frontmatter pattern from `local-users-basic-auth.md`:

    ```
    ---
    sidebar_label: LDAP Authentication
    sidebar_position: 4
    ---
    ```

    (Verify `sidebar_position: 4` does not collide with existing files in `docs/docs/authentication/`; if a conflict exists, use the next available integer.)

    The guide MUST contain these top-level sections (`## Heading` for each), in this order, with content derived from the source files listed in read_first:

    1. **Overview** — LDAP/AD authentication via the Basic Auth header; coexists with local_users (local checked first, LDAP fallback); uses a service-account bind followed by a per-user bind to validate credentials.

    2. **Configuration** — A complete YAML config block. Field names MUST be the mapstructure tags from `internal/config/config.go` lines 84-119. Include `enabled`, `url`, `bind_dn`, `bind_password`, `connection_timeout`, `tls_mode`, `tls_skip_verify`, `search_base`, `search_filter`, `group_search_base`, `group_search_filter`, `username_attribute`, `email_attribute`, `display_name_attribute`, `group_name_attribute`, `required_groups`.

    3. **TLS Modes** — Markdown table with three rows: `ldaps` (port 636, production), `starttls` (upgrade plain to TLS), `none` (plaintext, dev only — never production).

    4. **Attribute Mapping** — Document both ways to configure attribute mapping: per-field (`username_attribute`, `email_attribute`, `display_name_attribute`, `group_name_attribute`) and the `attribute_mapping` map. Source: config.go lines 106-115.

    5. **Group Search** — Optional; explain that omitting `group_search_base` and `group_search_filter` skips group lookup.

    6. **Required Groups** — Access gate: only users in at least one of `required_groups` are admitted.

    7. **Active Directory Example** — Complete YAML block matching the pattern in the pattern map (use `ldaps://ad.corp.yourdomain.com:636`, `sAMAccountName` filter, `keyline-users` required group).

    8. **OpenLDAP Example** — Complete YAML block (use `ldap://ldap.yourdomain.com:389`, `starttls`, `uid` filter, `attribute_mapping` form).

    9. **Username Normalisation** — Keyline trims whitespace, lower-cases, and replaces unsupported characters. Source: `internal/auth/ldap.go` lines 225-232.

    10. **Secure Credential Handling** — `bind_password` MUST be a `${ENV_VAR}` reference; plaintext values are rejected at startup. Source: `internal/auth/ldap.go` lines 115-128.

    11. **Troubleshooting** — Common errors (connection failure, bind failure, user not found, TLS verification failure) with the likely cause for each.

    All URL placeholders MUST follow the `yourdomain.com` pattern from D-06. Do NOT use `example.com` in any new content.
  </action>
  <verify>
    <automated>test -f docs/docs/authentication/ldap-authentication.md && rg -c "^sidebar_label: LDAP Authentication" docs/docs/authentication/ldap-authentication.md | grep -qx '1' && rg -c "^## " docs/docs/authentication/ldap-authentication.md | awk '$1 >= 8 {exit 0} {exit 1}' && rg -c "ldaps|starttls|none" docs/docs/authentication/ldap-authentication.md | grep -qv '^0$' && rg -c "bind_password" docs/docs/authentication/ldap-authentication.md | grep -qv '^0$' && rg -c "\\\$\\{[A-Z_]+\\}" docs/docs/authentication/ldap-authentication.md | grep -qv '^0$' && rg "auth\\.example\\.com|ldap\\.example\\.com" docs/docs/authentication/ldap-authentication.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f docs/docs/authentication/ldap-authentication.md` exits 0
    - Frontmatter line `sidebar_label: LDAP Authentication` is present exactly once
    - File contains at least 8 `## ` section headings (Overview, Configuration, TLS Modes, Attribute Mapping, Group Search, Required Groups, AD Example, OpenLDAP Example, Username Normalisation, Secure Credential Handling, Troubleshooting — at least 8 of these)
    - `rg "ldaps" docs/docs/authentication/ldap-authentication.md` matches at least once
    - `rg "starttls" docs/docs/authentication/ldap-authentication.md` matches at least once
    - `rg "\bnone\b" docs/docs/authentication/ldap-authentication.md` matches at least once (in TLS modes context)
    - `rg "bind_password" docs/docs/authentication/ldap-authentication.md` matches at least 2 times (Configuration block + Secure Credential Handling section)
    - `rg "\$\{[A-Z_]+\}" docs/docs/authentication/ldap-authentication.md` matches at least once (env-var pattern documented)
    - `rg "sAMAccountName" docs/docs/authentication/ldap-authentication.md` matches at least once (AD example present)
    - `rg "uid|attribute_mapping" docs/docs/authentication/ldap-authentication.md` matches at least once (OpenLDAP example present)
    - `rg "required_groups" docs/docs/authentication/ldap-authentication.md` matches at least once
    - `rg "trim|lower-case|normali[sz]" -i docs/docs/authentication/ldap-authentication.md` matches at least once (username normalisation documented)
    - `rg "auth\.example\.com|ldap\.example\.com" docs/docs/authentication/ldap-authentication.md` exits 1 (no example.com URLs per D-06)
    - `rg "yourdomain\.com" docs/docs/authentication/ldap-authentication.md` matches at least once
  </acceptance_criteria>
  <done>New LDAP guide exists with the required sections, accurate config fields matching the struct, both AD and OpenLDAP examples, and bind_password env-var enforcement documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in code) | Documentation only. However, the LDAP guide documents a security-relevant configuration (bind_password handling) and must accurately convey the env-var requirement, otherwise readers may put secrets in config files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information disclosure | ldap-authentication.md bind_password guidance | mitigate | Explicitly require `${ENV_VAR}` reference and call out that plaintext values are rejected at startup, matching the runtime enforcement in `internal/auth/ldap.go`. |
| T-02-06 | Tampering | ldap-authentication.md TLS modes section | mitigate | State that `none` is dev-only and must never be used in production; recommend `ldaps` for production. |
</threat_model>

<verification>
- `test -f docs/docs/authentication/ldap-authentication.md` exits 0
- `rg -c "LDAP" docs/docs/authentication/overview.md` is at least 2
- `rg "auth\.example\.com|ldap\.example\.com" docs/docs/authentication/ldap-authentication.md` exits 1
</verification>

<success_criteria>
- LDAP appears in the authentication overview table and the priority-order narrative.
- ldap-authentication.md exists with accurate field names, both AD and OpenLDAP examples, and explicit bind_password env-var guidance.
- All new URLs follow the `yourdomain.com` pattern (D-06).
</success_criteria>

<output>
Create `.planning/phases/02-documentation-update/02-03-SUMMARY.md` when done.
</output>
