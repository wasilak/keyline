---
phase: 02-documentation-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - RELEASE-NOTES.md
  - docs/docusaurus.config.js
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "README.md describes LDAP authentication, dynamic user management, role mapping, Redis caching, CORS, and circuit breaker as v2.0 features"
    - "README.md architecture diagram lists OIDC + Basic + LDAP as the auth engine providers"
    - "RELEASE-NOTES.md contains zero occurrences of `your-org`"
    - "RELEASE-NOTES.md lists LDAP authentication under the v2.0 New Features section"
    - "RELEASE-NOTES.md docker pull command uses the correct ghcr.io/wasilak/keyline image reference"
    - "docs/docusaurus.config.js declares the version label as `2.0.x (Latest)`"
    - "docs/docusaurus.config.js has exactly one top-level `markdown:` key combining mermaid, format, and onBrokenMarkdownLinks hook"
  artifacts:
    - path: "README.md"
      provides: "Updated v2.0 feature coverage and corrected example URLs"
    - path: "RELEASE-NOTES.md"
      provides: "Wasilak org references and LDAP feature entry"
    - path: "docs/docusaurus.config.js"
      provides: "Correct version label and merged markdown config block"
  key_links:
    - from: "README.md Key Features section"
      to: "LDAP, CORS, circuit breaker config field names"
      via: "inline mention matching `config/config.example.yaml` field names"
      pattern: "ldap|cors|max_concurrent"
    - from: "RELEASE-NOTES.md links"
      to: "https://github.com/wasilak/keyline"
      via: "GitHub URLs"
      pattern: "github\\.com/wasilak/keyline"
---

<objective>
Fix root-level documentation accuracy for v2.0: expand README v2.0 feature coverage inline (D-05), replace `example.com` config URLs with `auth.yourdomain.com` pattern (D-06), wipe `your-org` placeholders from RELEASE-NOTES.md (D-07), add the missing LDAP feature entry to RELEASE-NOTES.md, and fix two real bugs in docusaurus.config.js (stale version label, duplicate `markdown:` key dropping `onBrokenMarkdownLinks`).

Purpose: Satisfies DOC-01 (README accurately describes v2.0 features) and DOC-02 (RELEASE-NOTES no placeholder URLs/org refs).
Output: Three updated files, all org refs corrected, v2.0 features documented at the root level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-documentation-update/02-CONTEXT.md
@.planning/phases/02-documentation-update/02-PATTERNS.md
@README.md
@RELEASE-NOTES.md
@docs/docusaurus.config.js
@config/config.example.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand README.md v2.0 feature coverage inline</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state, 140 lines)
    - config/config.example.yaml (source of truth for field names and example values)
    - .planning/phases/02-documentation-update/02-PATTERNS.md (README section, lines 28-76)
    - .planning/phases/02-documentation-update/02-CONTEXT.md (D-05, D-06, specifics)
  </read_first>
  <action>
    Implement D-05 (expand existing sections inline; do NOT add a new top-level "What's New in v2.0" section) and D-06 (replace `auth.example.com` with `auth.yourdomain.com` pattern in config URLs).

    Concrete edits:
    1. Architecture diagram block (lines 16-18): change the `Auth[Auth Engine<br/>OIDC + Basic]` label to `Auth[Auth Engine<br/>OIDC + Basic + LDAP]`.
    2. "What changed from elastauth" comparison table: add a row covering LDAP — old elastauth path delegated to Authelia, v2.0 has built-in LDAP with three TLS modes.
    3. Minimal Configuration / Quick Start config snippet: change every `https://auth.example.com/auth/callback` occurrence to `https://auth.yourdomain.com/auth/callback`. Leave any `*@example.com` email values untouched (those are intentional test placeholders).
    4. "Key Features" bullet list: expand to cover, with one short sentence each (one bullet per feature): dynamic Elasticsearch user management, LDAP auth with TLS modes (`ldaps`/`starttls`/`none`), role mapping (pattern-based, groups→roles), AES-256-GCM encrypted credential caching, Redis session backend, circuit breaker on ES client (`server.max_concurrent`), CORS allowed origins (`server.cors.allowed_origins`), env-var enforcement for sensitive fields (must use `${ENV_VAR}` references).
    5. Reference the exact config field names from `config/config.example.yaml` when mentioning features (snake_case as in mapstructure tags). Use `server.cors.allowed_origins`, `server.max_concurrent`, `ldap.tls_mode`, `cache.encryption_key`, `session.backend` — verbatim.
    6. Do NOT restructure the file. Edit within the existing section structure (Headline → Architecture → What changed → Quick Start → Why Keyline → Key Features → Documentation → Development → License).
  </action>
  <verify>
    <automated>rg -c "OIDC \\+ Basic \\+ LDAP" README.md | grep -qv '^0$' && rg -c "auth\\.yourdomain\\.com/auth/callback" README.md | grep -qv '^0$' && rg "auth\\.example\\.com/auth/callback" README.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "OIDC \+ Basic \+ LDAP" README.md` matches at least once (architecture diagram updated)
    - `rg "auth\.example\.com/auth/callback" README.md` exits 1 (zero matches)
    - `rg "auth\.yourdomain\.com/auth/callback" README.md` matches at least once
    - `rg -i "ldap" README.md` matches at least 3 times (architecture, comparison table, key features)
    - `rg "cors|allowed_origins" README.md` matches at least once (CORS coverage)
    - `rg "max_concurrent|circuit breaker" README.md` matches at least once
    - `rg "dynamic user management" -i README.md` matches at least once
    - `rg "AES-256-GCM|credential cach" -i README.md` matches at least once
    - `rg "^## What's New in v2.0" README.md` exits 1 (no new top-level section, per D-05)
    - File still contains the existing sections: `## Quick Start`, `## Key Features`, `## Documentation`, `## License`
  </acceptance_criteria>
  <done>README.md describes all major v2.0 features inline; no `auth.example.com` placeholder URLs in config examples; architecture diagram reflects three auth providers.</done>
</task>

<task type="auto">
  <name>Task 2: Fix RELEASE-NOTES.md placeholder org refs and add missing LDAP feature entry</name>
  <files>RELEASE-NOTES.md</files>
  <read_first>
    - RELEASE-NOTES.md (current state, 406 lines — pay attention to lines 29-32, 110, 309, 319, 324, 334-336, 369-371, 406)
    - .planning/phases/02-documentation-update/02-PATTERNS.md (RELEASE-NOTES section, lines 79-117)
    - .planning/phases/02-documentation-update/02-CONTEXT.md (D-07, D-08)
    - docs/docs/deployment/docker.md (line 19 for correct image reference confirmation)
  </read_first>
  <action>
    Apply D-07 (all `your-org` → `wasilak`) and preserve D-08 (leave `example.com` inside YAML config snippet values).

    Concrete edits:
    1. Line 324: replace `https://github.com/your-org/keyline/releases/tag/v2.0.0` with `https://github.com/wasilak/keyline/releases/tag/v2.0.0`.
    2. Line 334: replace `https://github.com/your-org/keyline` with `https://github.com/wasilak/keyline`.
    3. Line 335: replace `https://github.com/your-org/keyline/tree/main/docs` with `https://github.com/wasilak/keyline/tree/main/docs`.
    4. Line 336: replace `https://github.com/your-org/keyline/issues` with `https://github.com/wasilak/keyline/issues`.
    5. Line 369: replace `https://github.com/your-org/keyline/issues` with `https://github.com/wasilak/keyline/issues`.
    6. Line 370: replace `https://github.com/your-org/keyline/discussions` with `https://github.com/wasilak/keyline/discussions`.
    7. Line 371 (security email): remove the line entirely (no canonical wasilak security email exists; safer to omit than fabricate). If the surrounding bullet list breaks readability, replace the line with `- Security issues: please open a private issue on GitHub`.
    8. Line 406: replace `https://github.com/your-org/keyline/compare/v1.0.0...v2.0.0` with `https://github.com/wasilak/keyline/compare/v1.0.0...v2.0.0`.
    9. Line 319 (docker pull): replace `docker pull keyline:v2.0.0` with `docker pull ghcr.io/wasilak/keyline:v2.0.0` (matches `docs/docs/deployment/docker.md` line 19).
    10. Add a new "New Features" entry for LDAP authentication. Place it in the v2.0.0 New Features section using the same `#### Title` + bullet list pattern as existing entries (e.g. the entry around lines 29-32). Title: `LDAP Authentication (Active Directory / OpenLDAP)`. Bullets must list: three TLS modes `ldaps`/`starttls`/`none`; configurable service account bind, user search, group search; username normalisation (trim, lower-case, unsupported-char removal); optional `required_groups` access gate; integration with dynamic user management and role mappings.
    11. Do NOT touch `testuser@example.com` (line ~110), `*@admin.example.com` (line ~309), or any other `example.com` inside YAML config snippet values — those are intentional placeholders per D-08.
  </action>
  <verify>
    <automated>rg "your-org" RELEASE-NOTES.md; test $? -eq 1 && rg -c "github\\.com/wasilak/keyline" RELEASE-NOTES.md | grep -qv '^0$' && rg -c "ghcr\\.io/wasilak/keyline:v2\\.0\\.0" RELEASE-NOTES.md | grep -qv '^0$' && rg -c "LDAP Authentication" RELEASE-NOTES.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `rg "your-org" RELEASE-NOTES.md` exits 1 (zero matches)
    - `rg "github\.com/wasilak/keyline" RELEASE-NOTES.md` matches at least 7 times (all replaced URLs)
    - `rg "ghcr\.io/wasilak/keyline:v2\.0\.0" RELEASE-NOTES.md` matches at least once (docker pull fix)
    - `rg "LDAP Authentication" RELEASE-NOTES.md` matches at least once (new feature entry added)
    - `rg "ldaps|starttls" RELEASE-NOTES.md` matches at least once (TLS mode bullet present)
    - `rg "required_groups" RELEASE-NOTES.md` matches at least once
    - `rg "testuser@example\.com" RELEASE-NOTES.md` still matches (D-08 — intentional YAML value preserved)
    - `rg "admin\.example\.com" RELEASE-NOTES.md` still matches (D-08 — intentional YAML value preserved)
    - `rg "security@your-org" RELEASE-NOTES.md` exits 1
  </acceptance_criteria>
  <done>RELEASE-NOTES.md is free of `your-org` placeholders, docker pull command is correct, LDAP feature is documented under v2.0 new features, and intentional YAML placeholder values are preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Fix docusaurus.config.js stale version label and duplicate markdown key</name>
  <files>docs/docusaurus.config.js</files>
  <read_first>
    - docs/docusaurus.config.js (current state, especially lines 11, 17-18, 23-24, 36-37, 52, 56, 94, 129)
    - .planning/phases/02-documentation-update/02-PATTERNS.md (docusaurus.config.js section, lines 340-376)
  </read_first>
  <action>
    Fix two real defects (both correctness bugs, not style):

    1. **Stale version label** (around line 56): replace `label: '1.0.x (Latest)',` with `label: '2.0.x (Latest)',`.
    2. **Duplicate `markdown:` key** (around lines 23-24 and 36-37): merge the two top-level `markdown:` object literals into a single object. The merged object MUST contain all three keys: `mermaid: true`, `format: 'mdx'`, and `hooks: { onBrokenMarkdownLinks: 'warn' }`. Remove the duplicate. Without this fix, JavaScript silently overwrites the first `markdown:` block and `onBrokenMarkdownLinks` is dropped.

    Do NOT touch any of the already-correct values: `organizationName: 'wasilak'`, `projectName: 'keyline'`, `editUrl`, navbar/footer GitHub links, or `url: 'https://wasilak.github.io'`.
  </action>
  <verify>
    <automated>rg -c "1\\.0\\.x \\(Latest\\)" docs/docusaurus.config.js; test $? -eq 1 && rg -c "2\\.0\\.x \\(Latest\\)" docs/docusaurus.config.js | grep -qv '^0$' && rg -c "^\\s*markdown:" docs/docusaurus.config.js | grep -qx '1' && rg -c "onBrokenMarkdownLinks" docs/docusaurus.config.js | grep -qv '^0$' && cd docs && node -e "require('./docusaurus.config.js')"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "1\.0\.x \(Latest\)" docs/docusaurus.config.js` exits 1 (stale label removed)
    - `rg "2\.0\.x \(Latest\)" docs/docusaurus.config.js` matches exactly once
    - `rg "^\s*markdown:" docs/docusaurus.config.js` matches exactly once (no duplicate top-level key)
    - `rg "onBrokenMarkdownLinks" docs/docusaurus.config.js` matches at least once (hook preserved in merged block)
    - `rg "mermaid:\s*true" docs/docusaurus.config.js` matches at least once (mermaid preserved)
    - `rg "format:\s*'mdx'" docs/docusaurus.config.js` matches at least once (format preserved)
    - `cd docs && node -e "require('./docusaurus.config.js')"` exits 0 (config still parses as valid JS)
    - `rg "organizationName:\s*'wasilak'" docs/docusaurus.config.js` still matches (untouched)
  </acceptance_criteria>
  <done>Version label reads 2.0.x (Latest), there is exactly one top-level `markdown:` block containing mermaid+format+onBrokenMarkdownLinks, and the config file still parses as valid JavaScript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only Markdown documentation and a Docusaurus config JS file. No authentication, session, cryptography, or access-control code is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | RELEASE-NOTES.md security contact line | accept | Removing fabricated `security@your-org.com` line rather than inventing an address avoids directing reports to an unmonitored mailbox. |
| T-02-02 | Tampering | docs/docusaurus.config.js | mitigate | Verification step runs `node -e "require('./docusaurus.config.js')"` so a malformed merge that breaks the JS object is caught immediately. |
</threat_model>

<verification>
- `rg "your-org" README.md RELEASE-NOTES.md docs/docusaurus.config.js` exits 1 across all three files
- `rg "1\.0\.x" docs/docusaurus.config.js` exits 1
- `cd docs && node -e "require('./docusaurus.config.js')"` exits 0
</verification>

<success_criteria>
- DOC-02 satisfied: no `your-org` strings remain in RELEASE-NOTES.md; all GitHub links point to `wasilak/keyline`.
- DOC-01 partially satisfied (root layer): README expanded inline for all v2.0 features per CONTEXT specifics.
- Docusaurus config bug fixes: version label is 2.0.x, `onBrokenMarkdownLinks` hook is preserved through the single `markdown:` block.
</success_criteria>

<output>
Create `.planning/phases/02-documentation-update/02-01-SUMMARY.md` when done.
</output>
