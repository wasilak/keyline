---
phase: 03-metrics-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/elasticsearch/client.go
  - internal/elasticsearch/circuit_breaker.go
  - internal/auth/metrics.go
  - internal/auth/ldap.go
  - docs/observability/metrics.md
autonomous: true
requirements:
  - METRICS-01
  - METRICS-02
  - METRICS-03
  - METRICS-04
  - METRICS-05

must_haves:
  truths:
    - "go build ./... completes without errors"
    - "ESAPICallsTotal counter incremented on create_user, get_user, delete_user operations"
    - "CircuitBreakerState gauge updated at all state transitions (Closed=0, Open=1, HalfOpen=2)"
    - "LDAPBindAttempts, LDAPSearchDuration, LDAPConnectionErrors defined and wired in ldap.go"
    - "docs/observability/metrics.md exists with full metric reference and PromQL examples"
  artifacts:
    - path: "internal/auth/metrics.go"
      provides: "LDAP metric definitions (METRICS-02)"
    - path: "internal/elasticsearch/client.go"
      provides: "ESAPICallsTotal wired for all ES operations (METRICS-01)"
    - path: "internal/elasticsearch/circuit_breaker.go"
      provides: "CircuitBreakerState gauge at all state transitions (METRICS-03)"
    - path: "docs/observability/metrics.md"
      provides: "Metric reference documentation (METRICS-05)"
---

<objective>
Add Prometheus metrics for ES user management operations, circuit breaker state, and LDAP operations.
Resolve a pre-existing circular import that blocked ESAPICallsTotal from being wired into client.go.
Document all v2.1 metrics in docs/observability/metrics.md.

Requirements satisfied: METRICS-01, METRICS-02, METRICS-03, METRICS-04, METRICS-05
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Move ESAPICallsTotal and wire ES + circuit breaker metrics</name>
  <action>
    - Move ESAPICallsTotal definition to internal/elasticsearch package (circular import fix)
    - Wire ESAPICallsTotal via named-return+defer in client.go: create_user, get_user, delete_user operations
    - Wire CircuitBreakerState gauge at all state transitions in circuit_breaker.go (Closed=0, Open=1, HalfOpen=2)
  </action>
  <done>ESAPICallsTotal wired in client.go; CircuitBreakerState gauge at all 3 state transitions; go build ./... clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add and wire LDAP metrics</name>
  <action>
    - Create internal/auth/metrics.go with LDAPBindAttempts{result}, LDAPSearchDuration, LDAPConnectionErrors
    - Wire LDAP metrics into ldap.go: dial errors → LDAPConnectionErrors; all 3 bind sites → LDAPBindAttempts; searchUser duration → LDAPSearchDuration
  </action>
  <done>internal/auth/metrics.go created; all 3 LDAP metric types wired in ldap.go.</done>
</task>

<task type="auto">
  <name>Task 3: Add metrics documentation</name>
  <action>
    - Create docs/observability/metrics.md with: full metric table (name, type, labels, description), PromQL examples, suggested alerting rules
    - All v2.1 metrics use keyline_ prefix (METRICS-04)
  </action>
  <done>docs/observability/metrics.md created covering all 5 new metrics with PromQL examples.</done>
</task>

</tasks>

<verification>
Phase complete when:
1. go build ./... exits 0
2. internal/auth/metrics.go exists with LDAP metric definitions
3. circuit_breaker.go sets CircuitBreakerState at all transitions
4. client.go increments ESAPICallsTotal for ES operations
5. docs/observability/metrics.md exists with complete metric reference
</verification>
