---
phase: 01-module-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - integration/redis_session_test.go
  - integration/observability_test.go
  - integration/forward_auth_test.go
  - integration/standalone_proxy_test.go
  - integration/basic_auth_test.go
  - integration/ldap_test.go
  - internal/session/session.go
  - internal/session/session_test.go
  - internal/transport/forward_auth.go
  - internal/transport/standalone.go
  - internal/transport/transport_test.go
  - internal/usermgmt/manager.go
  - internal/usermgmt/manager_test.go
  - internal/usermgmt/rolemapper.go
  - internal/usermgmt/rolemapper_test.go
  - internal/auth/engine.go
  - internal/auth/engine_test.go
  - internal/auth/basic.go
  - internal/auth/basic_test.go
  - internal/auth/ldap.go
  - internal/auth/ldap_test.go
  - internal/auth/oidc.go
  - internal/auth/oidc_test.go
  - internal/server/server.go
  - internal/server/server_test.go
  - internal/server/logout.go
  - internal/server/logout_test.go
  - internal/cache/cache.go
  - internal/cache/extended.go
autonomous: true
requirements:
  - MOD-01
  - MOD-02

must_haves:
  truths:
    - "go build ./... completes without errors"
    - "go test ./... passes with no regressions"
    - "No remaining references to 'yourusername' in any Go source file or go.mod"
    - "go.mod module directive is 'github.com/wasilak/keyline'"
    - "go version directive in go.mod remains 'go 1.26' (unchanged)"
  artifacts:
    - path: "go.mod"
      provides: "Correct module identity"
      contains: "module github.com/wasilak/keyline"
    - path: "internal/auth/engine.go"
      provides: "Representative Go source with updated import path"
    - path: "internal/server/server.go"
      provides: "Representative Go source with updated import path"
  key_links:
    - from: "go.mod"
      to: "all *.go files"
      via: "module directive consumed by Go toolchain at compile time"
      pattern: "module github\\.com/wasilak/keyline"
    - from: "*.go imports"
      to: "go.mod"
      via: "import path resolution by Go compiler"
      pattern: "github\\.com/wasilak/keyline"
---

<objective>
Rename the Go module identity from `github.com/yourusername/keyline` to `github.com/wasilak/keyline` across the entire codebase. This is a mechanical string replacement in go.mod and 30 Go source files.

Purpose: The project currently has a placeholder module path that prevents correct builds under the intended module identity. This rename makes the module path match the actual GitHub repository.

Output: Updated go.mod + all 30 affected .go source files. go.sum may change due to go mod tidy cleanup (expected and acceptable). No logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/piotrek/git/keyline/.planning/PROJECT.md
@/Users/piotrek/git/keyline/.planning/ROADMAP.md
@/Users/piotrek/git/keyline/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace module path in go.mod and all Go source files</name>
  <files>
    go.mod,
    integration/redis_session_test.go,
    integration/observability_test.go,
    integration/forward_auth_test.go,
    integration/standalone_proxy_test.go,
    integration/basic_auth_test.go,
    integration/ldap_test.go,
    internal/session/session.go,
    internal/session/session_test.go,
    internal/transport/forward_auth.go,
    internal/transport/standalone.go,
    internal/transport/transport_test.go,
    internal/usermgmt/manager.go,
    internal/usermgmt/manager_test.go,
    internal/usermgmt/rolemapper.go,
    internal/usermgmt/rolemapper_test.go,
    internal/auth/engine.go,
    internal/auth/engine_test.go,
    internal/auth/basic.go,
    internal/auth/basic_test.go,
    internal/auth/ldap.go,
    internal/auth/ldap_test.go,
    internal/auth/oidc.go,
    internal/auth/oidc_test.go,
    internal/server/server.go,
    internal/server/server_test.go,
    internal/server/logout.go,
    internal/server/logout_test.go,
    internal/cache/cache.go,
    internal/cache/extended.go
  </files>
  <read_first>
    go.mod — verify current module directive line reads exactly: module github.com/yourusername/keyline
    Any one representative file (e.g. internal/auth/engine.go) — confirm import block pattern before bulk replace
    internal/usermgmt/manager_test.go.backup — this file also contains the old path; exclude it from the replace (it is a backup file, not a build artifact — do NOT modify it)
  </read_first>
  <action>
    Perform a two-step portable bulk replacement using perl -pi -e (works on both macOS and Linux — do not use sed -i with or without '' as it behaves differently across platforms):

    Step 1 — Update go.mod module directive:
      perl -pi -e 's|github\.com/yourusername/keyline|github.com/wasilak/keyline|g' go.mod

    Step 2 — Update all .go source files in one pass:
      fd -e go -x perl -pi -e 's|github\.com/yourusername/keyline|github.com/wasilak/keyline|g' {}

    IMPORTANT: Do NOT modify internal/usermgmt/manager_test.go.backup — it is explicitly excluded from the build and its modification is not required.

    IMPORTANT: Do NOT change the 'go 1.26' version directive in go.mod. It is valid (confirmed: go1.26.3 locally, golang:1.26-alpine in Dockerfile, GO_VERSION: '1.26' in CI). MOD-02 is already satisfied — no change needed.

    Step 3 — Verify zero remaining occurrences:
      rg "yourusername" go.mod
      rg "yourusername" --include="*.go" .
    Both commands must return exit code 1 (no matches).
  </action>
  <verify>
    <automated>rg "yourusername" go.mod; echo "go.mod exit: $?"; rg "yourusername" --include="*.go" . ; echo "go files exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - go.mod line 1 reads exactly: module github.com/wasilak/keyline
    - go.mod line 3 reads exactly: go 1.26 (unchanged)
    - rg "yourusername" go.mod returns exit code 1 (no matches)
    - rg "yourusername" --include="*.go" . returns exit code 1 (no matches)
    - internal/usermgmt/manager_test.go.backup is NOT modified (git diff shows no change for this file)
  </acceptance_criteria>
  <done>All 30 .go source files and go.mod have the new module path. Zero occurrences of "yourusername" remain in any .go file or go.mod.</done>
</task>

<task type="auto">
  <name>Task 2: Run go mod tidy and verify build and test gates pass</name>
  <files>
    go.mod,
    go.sum
  </files>
  <read_first>
    go.mod — confirm Task 1 replacement is in place before running tidy
  </read_first>
  <action>
    Run the verification sequence in order. Do not skip steps or reorder them.

    Step 1 — Prune and reconcile module graph after path change:
      go mod tidy

    Note: go.sum will likely change (checksums added/removed for existing dependencies). This is expected and acceptable. Review the diff to confirm it only touches checksums for already-known packages — no new dependencies should appear.

    Step 2 — Confirm all packages compile:
      go build ./...
    Expected: no output, exit code 0.

    Step 3 — Confirm all tests pass:
      go test ./...
    Expected: all packages report ok or PASS, exit code 0.

    If go build ./... fails with "cannot find package" or "unknown import path", a .go file still references the old path — re-run Task 1 verification to find the missed file.
  </action>
  <verify>
    <automated>go mod tidy && go build ./... && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - go mod tidy exits 0
    - go build ./... exits 0 with no output
    - go test ./... exits 0 with all packages passing (no FAIL lines in output)
    - go.sum diff contains only checksum additions/removals for packages already present in go.mod (no new package names in go.sum)
  </acceptance_criteria>
  <done>go build ./... and go test ./... both exit 0. The module identity is fully corrected and the codebase builds and tests cleanly under github.com/wasilak/keyline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree → Go toolchain | go.mod and .go files are consumed by the Go compiler at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | go.mod, *.go files | accept | This phase makes intentional in-place edits. Changes are tracked in git and verified by go build — any accidental corruption surfaces immediately as a build error. |
| T-01-02 | Tampering | go.sum | accept | go mod tidy may update go.sum; diff review (acceptance_criteria) confirms no unexpected new dependencies are introduced. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs occur in this phase. Go toolchain already installed. N/A. |

**Security note:** This phase makes no security-relevant changes. No authentication, session management, input validation, cryptography, or access control code is modified. The rename is purely a textual change to a Go module path identifier.
</threat_model>

<verification>
Phase complete when:
1. rg "yourusername" go.mod → no matches
2. rg "yourusername" --include="*.go" . → no matches
3. go build ./... → exit 0, no output
4. go test ./... → exit 0, all packages pass
5. go.mod line 1 = module github.com/wasilak/keyline
6. go.mod line 3 = go 1.26 (unchanged)
</verification>

<success_criteria>
1. go build ./... completes without errors — MOD-01 confirmed
2. go test ./... passes with no regressions — MOD-01 confirmed
3. No remaining references to 'yourusername' in Go source files or go.mod — MOD-01 confirmed
4. go.mod Go version directive is unchanged at 'go 1.26' — MOD-02 already satisfied, verified not degraded
</success_criteria>

<output>
Create /Users/piotrek/git/keyline/.planning/phases/01-module-rename/01-01-SUMMARY.md when done.
</output>
