---
phase: 04-otel-depth
plan: 01
type: execute
wave: 1
depends_on: [03-metrics-expansion]
files_modified:
  - cmd/keyline/main.go
  - internal/auth/ldap.go
  - internal/usermgmt/manager.go
  - docs/observability/tracing.md
  - docs/observability/logging.md
autonomous: true
requirements:
  - OTEL-01
  - OTEL-02
  - OTEL-03

must_haves:
  truths:
    - "go build ./... completes without errors"
    - "OTel log bridge wired in main.go gated by otel_enabled config flag (OTEL-01)"
    - "ldap.go has ldap.bind and ldap.search spans with ctx threading (OTEL-02)"
    - "manager.go has cache.get, es.create_credentials, es.upsert_user, cache.set spans (OTEL-02)"
    - "docs/observability/tracing.md exists with full span inventory (OTEL-03)"
    - "docs/observability/logging.md exists with OTel log bridge documentation (OTEL-03)"
  artifacts:
    - path: "cmd/keyline/main.go"
      provides: "OTel log bridge initialization (OTEL-01)"
    - path: "internal/auth/ldap.go"
      provides: "LDAP bind and search spans (OTEL-02)"
    - path: "internal/usermgmt/manager.go"
      provides: "Cache and ES operation spans (OTEL-02)"
    - path: "docs/observability/tracing.md"
      provides: "Span inventory and trace documentation (OTEL-03)"
    - path: "docs/observability/logging.md"
      provides: "Log levels, fields, OTel bridge documentation (OTEL-03)"
---

<objective>
Add OTel log bridge (opt-in via otel_enabled) and instrument LDAP + user management internals with spans.
Document the full span inventory and OTel log configuration.

Requirements satisfied: OTEL-01, OTEL-02, OTEL-03
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Wire OTel log bridge in main.go (OTEL-01)</name>
  <action>
    - In cmd/keyline/main.go, after otelgo/tracing initialization, conditionally connect loggergo to OTel log exporter when otelInitialized == true
    - Gate on existing otel_enabled config flag — no new config keys
  </action>
  <done>OTel log bridge wired in main.go; only active when otelInitialized is true.</done>
</task>

<task type="auto">
  <name>Task 2: Add LDAP and user management spans (OTEL-02)</name>
  <action>
    - ldap.go: add ldap.bind span (service_account and user bind types) and ldap.search span with ldap.username attribute; thread ctx into searchUser
    - manager.go: add cache.get, es.create_credentials, es.upsert_user, cache.set inline spans with error recording
  </action>
  <done>All 4 span types added in ldap.go (ldap.bind, ldap.search) and manager.go (cache.get, es.create_credentials, es.upsert_user, cache.set).</done>
</task>

<task type="auto">
  <name>Task 3: Add OTel documentation (OTEL-03)</name>
  <action>
    - Create docs/observability/tracing.md: full span inventory table, example trace showing all spans end-to-end, exporter/sampling config
    - Create docs/observability/logging.md: log levels, standard fields, OTel log bridge setup, structured log examples
  </action>
  <done>docs/observability/tracing.md and docs/observability/logging.md created.</done>
</task>

</tasks>

<verification>
Phase complete when:
1. go build ./... exits 0
2. cmd/keyline/main.go contains OTel log bridge initialization
3. internal/auth/ldap.go contains ldap.bind and ldap.search span creation
4. internal/usermgmt/manager.go contains cache and ES operation spans
5. docs/observability/tracing.md and docs/observability/logging.md both exist
</verification>
