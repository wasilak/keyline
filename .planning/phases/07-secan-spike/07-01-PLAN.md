---
phase: 07-secan-spike
plan: 01
type: execute
wave: 1
depends_on: [06-auth-paths-docs]
files_modified:
  - docs/integrations/secan.md
autonomous: true
requirements:
  - SECAN-01

must_haves:
  truths:
    - "docs/integrations/secan.md exists with at least 2 topology options"
    - "Document covers forward-auth mode, proxy+injection mode, and hybrid mode"
    - "Option C (hybrid) is explicitly recommended with rationale"
    - "Limitations table present (multi-listener, credential API, etc.)"
    - "No Keyline or Secan source code changes — documentation only"
  artifacts:
    - path: "docs/integrations/secan.md"
      provides: "Secan integration architecture spike (SECAN-01)"
---

<objective>
Design spike: document how Secan (Rust+React ES management GUI) can be integrated with Keyline
for multi-user authentication and credential injection. Cover three topology options, recommend
Option C (hybrid), and document limitations and future scope.

Scope constraint: documentation and architecture design ONLY — no PoC or code changes to either
Secan or Keyline source.

Requirements satisfied: SECAN-01
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Research Secan architecture and document integration options</name>
  <action>
    - Research Secan (github.com/wasilak/secan): Rust backend, React frontend, no built-in multi-user auth
    - Document 3 topology options:
      - Option A: Forward-auth only (Traefik + Keyline forwardAuth → Secan; shared ES credentials)
      - Option B: Proxy+injection (Keyline standalone proxy injects per-user ES credentials)
      - Option C: Hybrid (Traefik forwardAuth for login + Keyline proxy for ES connections)
    - Recommend Option C with rationale
    - Document request sequence for Option C
    - Document limitations: multi-listener support needed, credential API deferred, gRPC stream future scope
    - Document config sketch for Option C deployment
  </action>
  <done>docs/integrations/secan.md created with 3 options, Option C recommendation, full request sequence, limitations table, future-scope section.</done>
</task>

</tasks>

<verification>
Phase complete when:
1. docs/integrations/secan.md exists
2. Three topology options documented
3. Option C recommended with rationale
4. Limitations table present
5. No source code modified
</verification>
