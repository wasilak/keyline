---
phase: 05-audit-logging
plan: 01
type: execute
wave: 1
depends_on: [04-otel-depth]
files_modified:
  - internal/auth/engine.go
  - docs/observability/audit.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "go build ./... completes without errors"
    - "EngineResult has AuthMethod string field set on all auth paths"
    - "logAuditEvent emits slog event with result, auth_method, username, source_ip, http_method, path on every auth decision"
    - "When OTel span is active in ctx, audit event includes trace_id and span_id (AUDIT-02)"
    - "docs/observability/audit.md exists with event format, field table, jq/LogQL examples"
    - "No credentials or secrets appear in audit log output"
  artifacts:
    - path: "internal/auth/engine.go"
      provides: "AuthMethod field + logAuditEvent wrapper (AUDIT-01, AUDIT-02)"
    - path: "docs/observability/audit.md"
      provides: "Audit log format reference documentation"
---

<objective>
Implement structured audit logging for every auth decision: add AuthMethod to EngineResult,
wrap Engine.Authenticate with logAuditEvent that emits slog fields, and inject OTel trace/span IDs
when an active span exists. Document the audit event format.

Requirements satisfied: AUDIT-01, AUDIT-02
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add AuthMethod to EngineResult and set on all auth paths</name>
  <action>
    - Add AuthMethod string field to EngineResult struct in engine.go
    - Set AuthMethod in all auth dispatch branches: session → "session", basic → "basic", ldap → "ldap", oidc → "oidc", fallthrough → "unknown"
  </action>
  <done>AuthMethod field added; all 5 auth paths set it before returning.</done>
</task>

<task type="auto">
  <name>Task 2: Implement logAuditEvent (AUDIT-01, AUDIT-02)</name>
  <action>
    - Add logAuditEvent function wrapping Engine.Authenticate in engine.go
    - Emits slog event: event=auth.decision, result, auth_method, username, source_ip, http_method, path
    - AUDIT-02: inject trace_id/span_id from trace.SpanFromContext(ctx) when span is recording
    - No credentials or secrets in log output
  </action>
  <done>logAuditEvent wired; emits on every auth decision; OTel correlation present when span is active.</done>
</task>

<task type="auto">
  <name>Task 3: Add audit documentation</name>
  <action>
    - Create docs/observability/audit.md with: event format, field table, jq/LogQL filter examples, OTel correlation notes, security constraints
  </action>
  <done>docs/observability/audit.md created.</done>
</task>

</tasks>

<verification>
Phase complete when:
1. go build ./... exits 0
2. EngineResult has AuthMethod field
3. logAuditEvent is called on every auth decision
4. When OTel span is active, trace_id and span_id appear in audit event
5. docs/observability/audit.md exists
</verification>
